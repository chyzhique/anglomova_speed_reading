body {
	margin: 1em auto;
	max-width: 40em;
	width: 88%;
}

summary {
	margin-bottom: 1em;
}

label,
input,
textarea {
	display: block;
	width: 100%;
}

label {
	font-weight: bold;
}

input,
textarea {
	margin-bottom: 1em;
}

textarea {
	min-height: 12em;
}

#reader-wrapper {
	align-items: center;
	display: flex;
	font-size: 2.5em;
	justify-content: center;
	min-height: 70vh;
}
